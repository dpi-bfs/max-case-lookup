<p>Hi BFS tech folk,</p>

<p>There was a critical error in the MaxCaseLookupApi. The following was presented to the user:</p>

<hr />
<blockquote>
{{{NotFoundResponseHtml}}}
</blockquote>
<hr />

<p>A critical error in this context is an error that someone needs to identify the cause of, and possibly fix.</p>

<p>Critical error possibilities include:</p>
<ul>
  <li>Max is down</li>
  <li>Power Automate is down</li>
  <li>An unanticipated error occurred</li>
</ul>

<p>Particulars:</p>
<ul>
  <li>OneBlinkEnvironment: {{OneBlinkEnvironment}}</li>
  <li>FormName: {{FormName}}</li>
  <li>FormId: {{FormId}}</li>
  <li>MaxCaseLookup_MaxEnvironment: {{MaxCaseLookup_MaxEnvironment}}</li>
  <li>MaxCaseLookup_MaxProjectName: {{MaxCaseLookup_MaxProjectName}}</li>
  <li>MaxCaseItemId: {{MaxCaseItemId}}</li>
</ul>

<p>To prevent your email inbox being flooded, subsequent critical error messages are not emailed for the next <strong>{{CriticalErrorCoolDownMinutes}} minutes</strong>, where sharing the following key: {{CriticalErrorCoolDownKey}} (FormId, MaxProjectName, MaxEnvironment). However all errors will be logged to the OneBlink console. See OneBlink Console > Developer Tools > API Hosting > nswfoodauthority-max-case-lookup.api.oneblink.io > {{OneBlinkEnvironment}}.</p>

<p>You've been emailed because you are listed in the notification list of the form's associated App (OneBlink Console > {{OneBlinkEnvironment}} > Apps > [Associated App] > Settings > Notifications and/or, if enabled in the App, Default Email Addresses).</p>